/* ─── CONFIG ─── */
const SERVER     = window.location.origin;
const W          = 640;
const H          = 480;
const INTERVAL   = 150;   // ms between /predict posts
const GAME_SECS  = 15;
const MIN_FRAMES = 10;
const CIRC       = 326.7; // 2π × 52 (ring circumference)